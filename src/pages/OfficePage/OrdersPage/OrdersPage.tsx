import { FC } from "react";

import styles from "./OrdersPage.module.scss";

export const OrdersPage: FC = (): JSX.Element => {
  return (
    <section>
      <h1>OrdersPage</h1>
    </section>
  );
};
